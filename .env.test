# Placeholder values loaded by react-native-dotenv under Jest (NODE_ENV=test),
# so tests and CI run without a real .env. Not real credentials.
SUPABASE_URL=https://example.supabase.co
SUPABASE_ANON_KEY=test-anon-key
